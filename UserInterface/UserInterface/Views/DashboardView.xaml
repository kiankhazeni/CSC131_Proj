<UserControl x:Class="UserInterface.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="DashboardToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Width" Value="62"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="SwitchTrack" Background="{StaticResource VitalsRedBrush}" CornerRadius="13"/>
                            <TextBlock x:Name="SwitchText" Text="Off" Foreground="{StaticResource VitalsWhiteBrush}" FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0"/>
                            <Ellipse x:Name="SwitchThumb" Width="20" Height="20" Fill="{StaticResource VitalsWhiteBrush}" HorizontalAlignment="Left" Margin="3,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchTrack" Property="Background" Value="{StaticResource VitalsGreenBrush}"/>
                                <Setter TargetName="SwitchText" Property="Text" Value="On"/>
                                <Setter TargetName="SwitchText" Property="HorizontalAlignment" Value="Left"/>
                                <Setter TargetName="SwitchText" Property="Margin" Value="8,0,0,0"/>
                                <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="SwitchThumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,4,0">
        <StackPanel Margin="5">

            <Border Background="{StaticResource VitalsPanelBrush}" BorderBrush="{StaticResource VitalsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Automation Status" FontSize="12" Foreground="{StaticResource VitalsMutedTextBrush}"/>
                        <TextBlock Text="{Binding AutomationStatusText}" FontSize="22" FontWeight="SemiBold" Foreground="{StaticResource VitalsWhiteBrush}"/>
                    </StackPanel>

                    <Button Grid.Column="1" Content="Start" Command="{Binding StartAutomationCommand}" Width="90" Height="32" VerticalAlignment="Center" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="{StaticResource VitalsWhiteBrush}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{StaticResource VitalsGreenBrush}" CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Grid.Column="3" Content="Stop" Command="{Binding StopAutomationCommand}" Width="90" Height="32" VerticalAlignment="Center" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="{StaticResource VitalsWhiteBrush}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{StaticResource VitalsRedBrush}" CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <UniformGrid Columns="3" Margin="0,0,0,12">
                <Border Background="{StaticResource VitalsPanelBrush}" BorderBrush="{StaticResource VitalsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Completed Registrations" FontSize="12" Foreground="{StaticResource VitalsMutedTextBrush}"/>
                        <TextBlock Text="{Binding ActiveStudentsCount}" FontSize="26" FontWeight="SemiBold" Foreground="{StaticResource VitalsWhiteBrush}"/>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource VitalsPanelBrush}" BorderBrush="{StaticResource VitalsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Needs Follow-up" FontSize="12" Foreground="{StaticResource VitalsMutedTextBrush}"/>
                        <TextBlock Text="{Binding PendingStudentsCount}" FontSize="26" FontWeight="SemiBold" Foreground="{StaticResource VitalsWhiteBrush}"/>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource VitalsPanelBrush}" BorderBrush="{StaticResource VitalsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                    <StackPanel>
                        <TextBlock Text="Logged Errors" FontSize="12" Foreground="{StaticResource VitalsMutedTextBrush}"/>
                        <TextBlock Text="{Binding ErrorCount}" FontSize="26" FontWeight="SemiBold" Foreground="{StaticResource VitalsErrorBrush}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.15*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource VitalsPanelBrush}" BorderBrush="{StaticResource VitalsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15">
                    <StackPanel>
                        <TextBlock Text="System Activity" FontWeight="SemiBold" FontSize="15" Foreground="{StaticResource VitalsWhiteBrush}" Margin="0,0,0,10"/>
                        <DataGrid ItemsSource="{Binding ModuleStatuses}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="None"
                                  Background="{StaticResource VitalsPanelBrush}"
                                  RowBackground="{StaticResource VitalsPanelBrush}"
                                  AlternatingRowBackground="{StaticResource VitalsPanelAltBrush}"
                                  Foreground="{StaticResource VitalsWhiteBrush}"
                                  BorderThickness="0"
                                  RowHeaderWidth="0"
                                  Height="140"
                                  VerticalScrollBarVisibility="Disabled"
                                  PreviewMouseWheel="ForwardMouseWheelToParentScrollViewer">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="{StaticResource VitalsFieldBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource VitalsBodyTextBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="8,6"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource VitalsWindowBackgroundBrush}"/>
                                            <Setter Property="Foreground" Value="{StaticResource VitalsWhiteBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Module" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Last Run" Binding="{Binding LastRunText}" Width="95"/>
                                <DataGridTextColumn Header="Next Run" Binding="{Binding NextRunText}" Width="95"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="{StaticResource VitalsPanelBrush}" BorderBrush="{StaticResource VitalsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Automation Modules" FontWeight="SemiBold" FontSize="15" Foreground="{StaticResource VitalsWhiteBrush}" Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding ModuleStatuses}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="95"/>
                                            <ColumnDefinition Width="70"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" Foreground="{StaticResource VitalsLightTextBrush}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Status}" Foreground="{StaticResource VitalsBodyTextBrush}" VerticalAlignment="Center"/>
                                        <CheckBox Grid.Column="2" IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DashboardToggleSwitchStyle}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Background="{StaticResource VitalsPanelBrush}" BorderBrush="{StaticResource VitalsBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15">
                <StackPanel>
                    <DockPanel Margin="0,0,0,10">
                        <TextBlock Text="Recent Logs" FontWeight="SemiBold" FontSize="15" Foreground="{StaticResource VitalsWhiteBrush}" DockPanel.Dock="Left"/>
                    </DockPanel>
                    <ItemsControl ItemsSource="{Binding RecentLogs}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource VitalsBorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,8" Margin="0,0,0,8">
                                    <StackPanel>
                                        <DockPanel Margin="0,0,0,3">
                                            <TextBlock Text="{Binding Source}" FontWeight="SemiBold" Foreground="{StaticResource VitalsWhiteBrush}" DockPanel.Dock="Left"/>
                                            <TextBlock Text="{Binding TimeText}" Foreground="{StaticResource VitalsMutedTextBrush}" FontSize="11" HorizontalAlignment="Right"/>
                                        </DockPanel>
                                        <TextBlock Text="{Binding Message}" Foreground="{StaticResource VitalsBodyTextBrush}" FontSize="12" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding Level}" Foreground="{StaticResource VitalsMutedTextBrush}" FontSize="11" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
