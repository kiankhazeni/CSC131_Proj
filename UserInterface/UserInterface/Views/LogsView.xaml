<UserControl x:Class="UserInterface.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <DataGrid ItemsSource="{Binding LogEntries}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding TimeText}" Width="1.2*"/>
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="1.1*"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="2.2*"/>
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="0.8*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>