<UserControl x:Class="UserInterface.Views.RemindersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Border Background="{StaticResource VitalsPanelBrush}" CornerRadius="8" BorderBrush="{StaticResource VitalsBorderBrush}" BorderThickness="1" ClipToBounds="True">
            <DataGrid ItemsSource="{Binding ReminderItems}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      Background="{StaticResource VitalsTransparentBrush}"
                      RowBackground="{StaticResource VitalsPanelBrush}"
                      AlternatingRowBackground="{StaticResource VitalsPanelAltBrush}"
                      Foreground="{StaticResource VitalsWhiteBrush}"
                      BorderThickness="0"
                      RowHeaderWidth="0"
                      FontSize="13"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">

                <DataGrid.Resources>
                    <Style x:Key="CellTextWithTooltipStyle" TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{StaticResource VitalsWhiteBrush}"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                        <Setter Property="ToolTipService.InitialShowDelay" Value="1000"/>
                        <Setter Property="ToolTipService.ShowDuration" Value="20000"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource VitalsFieldBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource VitalsMutedTextBrush}"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource VitalsBorderBrush}"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource VitalsWindowBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource VitalsWhiteBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn ElementStyle="{StaticResource CellTextWithTooltipStyle}" Header="TYPE" Binding="{Binding Type}" Width="1*"/>
                    <DataGridTextColumn ElementStyle="{StaticResource CellTextWithTooltipStyle}" Header="STUDENT" Binding="{Binding StudentName}" Width="1*"/>
                    <DataGridTextColumn ElementStyle="{StaticResource CellTextWithTooltipStyle}" Header="EMAIL" Binding="{Binding Email}" Width="1.6*"/>
                    <DataGridTextColumn ElementStyle="{StaticResource CellTextWithTooltipStyle}" Header="DATE" Binding="{Binding DateText}" Width="0.8*"/>
                    <DataGridTextColumn ElementStyle="{StaticResource CellTextWithTooltipStyle}" Header="DUE" Binding="{Binding DueText}" Width="0.8*"/>
                    <DataGridTextColumn ElementStyle="{StaticResource CellTextWithTooltipStyle}" Header="STATUS" Binding="{Binding Status}" Width="1.3*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>