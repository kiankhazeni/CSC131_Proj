<UserControl x:Class="UserInterface.Views.RemindersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <DataGrid ItemsSource="{Binding ReminderItems}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="1.0*"/>
                <DataGridTextColumn Header="Student" Binding="{Binding StudentName}" Width="1.2*"/>
                <DataGridTextColumn Header="Contact" Binding="{Binding Contact}" Width="1.6*"/>
                <DataGridTextColumn Header="Due" Binding="{Binding DueText}" Width="1.0*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="0.9*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>